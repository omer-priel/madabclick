version: 0.0

os: linux

files:
  - source: /frontend/
    destination: /var/frontend/
    overwrite: true

file_exists_behavior: OVERWRITE

hooks:
  ApplicationStart:
    - location: scripts/aws/application-start.sh
      timeout: 1200
      runas: root
